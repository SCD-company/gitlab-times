import { Styled as fgs } from './FiltersGrouping.styled';

const Label = fgs.Label;

const Container = fgs.Container;

const Checkbox = fgs.Checkbox;

const Item = fgs.Item;

const FullWidthContainer = fgs.FullWidthContainer;

export const Styled = {
  Container,
  Checkbox,
  Label,
  Item,
  FullWidthContainer,
};

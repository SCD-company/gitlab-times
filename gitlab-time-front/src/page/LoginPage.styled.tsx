import styled from 'styled-components';
import { Styled as S } from './Page.Styled';

const Page = S.Page;

const Label = S.Label;

const Panel = styled.div`
  text-align: center;
  margin-top: 300px;
  font-size: 20px;
`;
export const Styled = {
  Page,
  Label,
  Panel,
};

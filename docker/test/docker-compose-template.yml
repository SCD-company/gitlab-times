#This is a TEMPLATE for TEST docker file
# Before starting it
# 1) Make a copy of it named docker-compose.yml
# 2) In the copy, teplace all the values in {} with your actual values before running it
# 3) Place a dump of your gitlab database inside ./scripts directory and name it gitlab_dump.sql
version: "3.3"

services:
  db-postgresql:
    image: postgres:13
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=123
      - POSTGRES_DB=gitlab_time
    volumes:
      - ./../scripts/gitlab_dump.sql:/docker-entrypoint-initdb.d/gitlab_dump.sql

  backend:
    image: scdcompany/gitlab-time-back:latest
    restart: always
    ports:
      - 8080:8080
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-postgresql:5432/gitlab_time
      - SPRING_DATASOURCE_USERNAME=postgres
      - SPRING_DATASOURCE_PASSWORD=123
      - GITLAB_CLIENT_CLIENTID={INSERT ID HERE}
      - GITLAB_CLIENT_CLIENTSECRET={INSERT SECRET HERE}
      - GITLAB_CLIENT_ACCESSTOKENURI=https://{yourgitlab.com}/oauth/token
      - GITLAB_CLIENT_USERAUTHORIZATIONURI=https://{yourgitlab.com}/oauth/authorize
      - GITLAB_RESOURCE_USERINFOURI=https://{yourgitlab.com}/api/v4/user
    depends_on:
      - db-postgresql

  frontend:
    image: scdcompany/gitlab-time-front:latest
    restart: always
    ports:
      - 3000:80